# Meshy 7 skier — selected and integrated

The candidate is active in the game. See `docs/presentation/SKIER.md` at the project root for preparation, posture, validation, limitations and rebuild instructions.

- `reference.png`: built-in image generation reference; exact prompt in `reference_prompt.txt`.
- `candidate.glb` and `candidate_textures/`: untouched textured Meshy 7 Ultra source.
- `rig.glb`: untouched Meshy rigging output.
- `qa/`: original and reduced Blender renders and round-trip statistics.
- `runtime_qa.json`: verified body and boot exports.
- `credit_ledger.json`: 40 credits spent from the authorized 1,000-credit allowance.
- `previous/`: original runtime skier and preparation/pose scripts for comparison.

Editable assembled source: `art_source/blender/skier_v7.blend`.
Runtime: `assets/graphics/models/skier_v7.glb` and `skier_v7_boot_left/right.glb`.
Matched final views: `artifacts/skier_v7/final/`.
Movement clip: `artifacts/skier_v7/skier_v7_motion.mp4`.
